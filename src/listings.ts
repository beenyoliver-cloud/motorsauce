/** Temporary stub to satisfy legacy imports during the DB-only migration. */
export async function getAllListings() {
  return []; // no mocks; callers will just see an empty list
}
export default [];
